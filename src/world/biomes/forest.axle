// Forest — dense oak woodland on green grass.

use crate::world::biomes::Biome;
use crate::config;
use crate::config::{ TreeKind };

class Forest : Biome {
    /** Construct the forest biome. */
    constructor() {
        super();
        self.treeThresh = 0.928;
        self.treeKindId = TreeKind::Oak;
        self.treeHt = 4;
    }
}
