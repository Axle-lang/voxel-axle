// Snowy — snow-covered ground with hardy spruces.

use crate::world::biomes::Biome;
use crate::config;
use crate::config::{ Block, TreeKind };

class Snowy : Biome {
    /** Construct the snowy biome. */
    constructor() {
        super();
        self.treeThresh = 0.984;
        self.treeKindId = TreeKind::Spruce;
        self.treeHt = 6;
    }

    /**
     * Top block: snow.
     * @param height  the column's surface height
     * @return the surface block id
     */
    fn surface(self, height : i32) : Block {
        return Block::Snow;
    }
}
